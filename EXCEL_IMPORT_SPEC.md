# 나이스 엑셀 인식 규칙

## 실제 파일을 먼저 봤다

이 규칙은 추측이 아니라 실제 나이스 «교과시간별출석부» 파일 4개를 열어 구조를 확인한 뒤
작성했습니다. 확인된 구조는 다음과 같습니다.

| 위치 | 내용 |
| --- | --- |
| 시트 | `sheet1` 1개, 범위 `A1:AE37` |
| 2행 T열 (병합 T2:AC2) | 생성일 `2026.08.12.` |
| 4행 F열 (병합 F4:X4) | 제목 `교과시간별출석부` |
| 6행 C열 (병합 C6:S6) | `교과 : 통합과학1 1학년  1-1` ← **학년과 반 사이 공백 2개** |
| 6행 T열 (병합 T6:AC6) | `담당교사 : ○○○` |
| 7행 C열 | `[월 6], [화 1], [수 5]` |
| **9행 = 헤더** | B`연번`(B:C병합) · D`학년` · E`학과`(E:F병합) · G`반` · H`번호` · **I`성 명`** |
| 10~34행 | 학생 25명. 숫자는 전부 **부동소수(`1.0`)** |
| 36행 | J`1 / 1` (페이지 번호) · W`○○고등학교` (인쇄 푸터) |

여기서 곧바로 드러나는 함정이 셋입니다.

1. **A열이 비어 있고 표가 B열부터 시작합니다.** 열 인덱스를 고정하면 전부 어긋납니다.
2. **헤더 행에 빈 문자열 셀이 20개 넘게 있습니다** (J9~AB9, 출석 체크란).
   "값이 있는 셀이 가장 많은 행"으로 헤더를 찾으면 실패합니다.
3. **번호가 정수가 아니라 `1.0` 입니다.** 문자열로 바꾸면 `"1.0"` 이 되어 버립니다.

같은 폴더의 다른 파일 하나는 **한셀(HCell)로 저장된 xlsx** 였는데, 구조가 완전히 달랐습니다
(헤더가 2행, 번호·이름이 A·B열, 메타 정보 없음, 31명). Python의 openpyxl은 이 파일을
아예 열지 못했습니다. 아래 규칙은 이 파일도 그대로 읽습니다.

---

## 1단계 — 헤더 행 찾기 (점수 기반)

고정된 셀 주소도, 고정된 시작 행도 쓰지 않습니다. **모든 시트의 모든 행**에 점수를 매깁니다.

### 정규화

비교 전에 셀 값을 이렇게 다듬습니다.

```
앞뒤 공백 제거 → 연속 공백 축소 → 줄바꿈 제거 → NFC 정규화 → 모든 공백 제거
```

그 결과 `성 명`, `성  명`, `성\n명`, `성명` 이 전부 같은 `성명` 이 됩니다.
비단 공백(NBSP), 전각 공백, 제로폭 공백도 함께 처리합니다.

### 인식하는 낱말

| 항목 | 인식하는 표기 | 점수 |
| --- | --- | --- |
| 이름 | 성명 · 이름 · 성함 · 학생명 · 학생이름 · name | **4 (필수)** |
| 번호 | 번호 · 출석번호 · 번 · 학번 · no | **4 (필수)** |
| 연번 | 연번 · 순번 · 일련번호 | 2 |
| 학년 | 학년 | 1 |
| 학과 | 학과 · 계열 · 전공 | 1 |
| 반 | 반 · 학반 · 분반 | 1 |
| 성별 | 성별 · 성 | 1 |

**이름과 번호가 모두 있어야만** 헤더 후보가 됩니다. 나머지는 가산점입니다.
비교는 **완전 일치**라서 `성별`이 `성명` 열을 가로채지 않습니다.
빈 문자열은 점수가 0이므로 출석 체크란은 자연히 무시됩니다.

`번호` 열이 없고 `연번` 만 있으면 연번을 출석번호로 씁니다.

실제 파일에서는 9행이 `연번·학년·학과·반·번호·성 명` 을 모두 갖춰 **13점**으로
최고점을 받아 헤더로 채택됩니다.

### 여러 개가 나오면

한 시트에 여러 학급이 세로로 쌓여 있을 수 있으므로 **후보를 전부 수집**하고,
각각을 다음 헤더 직전까지 읽어 별도의 명렬표로 만듭니다. 시트가 여러 개면 시트마다 반복합니다.
숨김 시트도 읽되, 선택 목록에서 «숨김» 표시와 함께 뒤로 보냅니다.

---

## 2단계 — 학생 행 읽기와 끝 판정

헤더 다음 줄부터 읽어 내려가다 아래 조건에서 멈춥니다.

| 종료 조건 | 실제 파일에서 |
| --- | --- |
| 번호와 이름이 **모두 빈 행이 연속 3개** | 35행 이후 |
| `1 / 1`, `2/3`, `- 1 -` 형태의 페이지 번호 | 36행 J열 |
| `○○학교` 로 끝나는 짧은 행 | 36행 W열 |
| `계`, `합계`, `소계`, `총계`, `인원`, `비고` 만 있는 행 | 교사가 덧붙인 집계 줄 |
| 다음 헤더 행 | 여러 학급이 쌓여 있을 때 |

푸터 판정은 «값 있는 셀이 4개 이하이고, 그것들이 전부 인쇄 부산물일 때»만 적용합니다.
학생 행이 우연히 걸리지 않게 하기 위해서입니다.

**표 중간의 빈 행은 넘어갑니다.** 연속 3개가 되어야 표가 끝난 것으로 봅니다.

---

## 3단계 — 값 해석

### 번호

```
1        → 1        (정수)
1.0      → 1        (부동소수. 나이스 기본형)
"1.0"    → 1        (문자열)
"01"     → 1
" 3 "    → 3
"１２"    → 12       (전각 숫자)
"12번"   → 12
1.5      → null     (정수가 아니면 번호가 아님)
"가나"    → null
```

번호를 읽지 못해도 **행을 버리지 않습니다.** 학생으로 등록하고 «번호를 읽지 못했습니다»
경고를 띄워 교사가 명단 화면에서 채우게 합니다.

### 이름

내부 공백만 한 칸으로 줄이고 원문을 유지합니다. **이름이 비면 그 행은 건너뛰고** 경고합니다.

### 성별

**절대 이름으로 추정하지 않습니다.** `성별` 열이 매핑되었을 때만 읽습니다.

```
남 · 남자 · 남학생 · M · male · 1  →  남
여 · 여자 · 여학생 · F · female · 2 →  여
기타 · other                       →  기타
비공개                              →  비공개
그 외 · 빈 칸                       →  미지정
```

나이스 명렬표에는 성별 열이 없으므로 실제로는 전원 «미지정»으로 들어옵니다.
남녀 섞기 기능은 교사가 직접 입력해야 동작하며, 미지정이 많으면 그 사실을 경고로 알립니다.

### 식별자

행마다 `crypto.randomUUID()` 를 부여합니다. 동명이인이 있어도 완전히 별개의 학생이 되며,
«이름이 같은 학생이 있습니다. 서로 다른 학생으로 구분해 두었습니다» 라고 안내합니다.

---

## 4단계 — 메타 정보 (실패해도 무방)

헤더 위쪽과 표 아래 4줄에서 다음을 찾습니다. **하나도 못 찾아도 명단 가져오기는 정상 진행됩니다.**

| 항목 | 패턴 |
| --- | --- |
| 생성일 | `2026.08.12.` · `2026-08-12` · `2026/8/12` |
| 학교명 | `○○초등학교` · `○○중학교` · `○○고등학교` 로 끝나는 셀 |
| 교과·학년·반 | `교과 : (.+)` 에서 `(\d+)학년` 과 `(\d+)-(\d+)` 를 추출 |
| 담당교사 | `담당교사 : (.+)` |
| 수업시간 | `[월 6], [화 1], [수 5]` |

학년과 반은 **학생 행의 `학년`·`반` 열 값을 우선**합니다. 모든 행이 같은 값을 가질 때만
채택하며, 제목 줄 파싱보다 신뢰도가 높기 때문입니다.

---

## 5단계 — 자동 인식이 실패하면

헤더를 찾지 못하면 오류로 끝내지 않고 **수동 매핑 화면**으로 넘어갑니다.
시트의 앞 20줄을 표로 보여 주고, 교사가 헤더 줄 번호와 번호·이름 열을 직접 고르면
같은 파싱 규칙이 그 위치에 적용됩니다. 자동 인식이 엉뚱한 표를 골랐을 때도
«직접 지정하기» 버튼으로 같은 화면에 들어갈 수 있습니다.

---

## 견디도록 만든 상황

| 상황 | 처리 |
| --- | --- |
| 헤더 위에 여러 줄의 제목 | 점수 기반 탐색이므로 무관 |
| 병합 셀 | 병합은 왼쪽 위 셀에만 값이 있고, 그 위치를 그대로 읽음 |
| `성 명` / `성명` 표기 차이 | 공백 제거 후 비교 |
| 열 순서가 다름 | 위치가 아니라 낱말로 찾음 |
| 열이 추가되거나 빠짐 | 이름·번호만 있으면 동작 |
| 숫자와 문자열 혼재 | `parseNumberLike` 가 흡수 |
| 표 중간 빈 행 | 연속 3개까지 통과 |
| 표 아래 학교명·페이지 번호 | 푸터로 판정해 종료 |
| 시트 여러 개 | 전부 조사해 선택지로 제시 |
| 숨김 시트·숨김 행 | 읽되 숨김 표시 |
| 동명이인 | UUID로 분리 |
| 중복 번호 | 경고하되 버리지 않음 |
| 빈 이름 | 건너뛰고 경고 |
| 손상된 xlsx | «손상되어 열 수 없습니다» + 다시 저장 안내 |
| 엑셀이 아닌 파일 | «엑셀로 읽을 수 없는 형식입니다» |
| 암호 걸린 파일 | «암호를 푼 뒤 다시 시도해 주세요» |
| 정체불명 바이트 | SheetJS가 예외 없이 파싱하므로 «명렬표 0개» → 수동 매핑 |
| 파일을 연달아 여러 번 업로드 | 입력을 매번 초기화하고 ArrayBuffer 참조를 즉시 해제 |
| CSV (UTF-8 / EUC-KR) | UTF-8로 먼저 시도하고, 대체 문자가 보이면 EUC-KR로 재해석. BOM 제거 |

---

## 관련 코드와 테스트

| 파일 | 역할 |
| --- | --- |
| `src/core/excel/detectHeader.ts` | 점수 기반 헤더 탐색 |
| `src/core/excel/parseRoster.ts` | 행 읽기·종료 판정·검증 |
| `src/core/excel/meta.ts` | 제목 줄 파싱 |
| `src/core/excel/importRoster.ts` | 시트·블록을 명렬표 후보로 |
| `src/core/excel/readWorkbook.ts` | SheetJS를 쓰는 유일한 파일 |
| `tests/unit/excel.test.ts` | 28개 테스트 |
| `tests/support/neisFixture.ts` | 실제 구조를 재현한 합성 픽스처 |

픽스처는 실제 파일의 특징(A열 공백, 병합, 부동소수 번호, 빈 헤더 20여 개, `1 / 1` 푸터,
학교명 푸터)을 그대로 재현하되 이름은 `학생01`~`학생25` 입니다.
**실제 학생 이름은 이 저장소 어디에도 없습니다.**

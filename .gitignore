node_modules/
dist/
dist-ssr/
coverage/
.vite/
*.local
.DS_Store
Thumbs.db

# Playwright
test-results/
playwright-report/
blob-report/
.playwright/

# --------------------------------------------------------------------------
# Never commit real rosters. Student data must not enter version control.
# Test fixtures are generated at run time from synthetic names instead.
# --------------------------------------------------------------------------
*.xlsx
*.xls
*.xlsm
*.csv
!tests/**/*.generated.xlsx
/private/

# --------------------------------------------------------------------------
# Local working notes. The session handoff records the author's school, the
# subject they teach and absolute paths to real roster files — harmless on this
# machine, identifying once the repository is public. Kept on disk, out of git.
# --------------------------------------------------------------------------
SESSION_HANDOFF.md

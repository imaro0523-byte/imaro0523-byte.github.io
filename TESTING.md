# 테스트

## 실행

```bash
npm test
```

```bash
npm run test:e2e:install
```

```bash
npm run test:e2e
```

```bash
npm run typecheck
```

---

## 테스트에는 실제 학생 이름이 없다

모든 픽스처는 `학생01`~`학생25` 같은 **합성 데이터**입니다.
실제 명렬표의 이름은 코드·테스트·문서·스냅샷 어디에도 복사하지 않았습니다.

Playwright의 스크린샷·비디오·트레이스 기록도 꺼 두었습니다.
실패한 테스트의 아티팩트에 화면 속 명단이 남는 것을 막기 위해서입니다.

`tests/support/neisFixture.ts` 는 실제 나이스 파일의 **구조**만 재현합니다 —
A열이 빈 채로 B열부터 시작하는 표, 병합 셀, 부동소수 번호(`1.0`),
성 명 오른쪽의 빈 문자열 헤더 20여 개, `1 / 1` 페이지 번호와 학교명 푸터.

---

## 단위 테스트 (124개)

### `excel.test.ts` — 28개

| 확인하는 것 |
| --- |
| 실제 구조의 시트에서 헤더 행을 9행으로 정확히 찾는다 |
| 출석 체크란의 빈 문자열 헤더 20여 개에 속지 않는다 |
| 표를 5행 4열 옮겨도 똑같이 찾는다 (고정 위치 가정 없음 증명) |
| `성 명` 과 `성명` 을 같게 취급한다 |
| 이름이나 번호 열이 없는 행은 헤더로 인정하지 않는다 |
| `번호` 가 없으면 `연번` 을 출석번호로 쓴다 |
| `성별` 이 `성명` 열을 가로채지 않는다 |
| 한 시트에 두 학급이 쌓여 있으면 둘 다 찾는다 |
| 25명을 읽고 인쇄 푸터 앞에서 멈춘다 |
| 표 중간의 빈 행을 넘어간다 |
| 번호가 부동소수·문자열·혼재여도 모두 정수로 읽는다 |
| `1.0` → `1`, `01` → `1`, `１２` → `12`, `12번` → `12`, `1.5` → null |
| 동명이인에게 서로 다른 UUID를 준다 |
| 중복 번호를 경고하되 행을 버리지 않는다 |
| 빈 이름 행은 건너뛰고 경고한다 |
| **이름으로 성별을 추정하지 않는다** (전원 `미지정`) |
| 성별 열이 매핑됐을 때만 성별을 읽는다 |
| 메타 정보(학교·교과·학년·반·교사·수업시간·생성일)를 뽑는다 |
| 메타가 전혀 없어도 명단은 정상 인식한다 |
| 숨김 시트보다 보이는 시트를 앞에 둔다 |
| 수동 매핑으로도 읽을 수 있다 |
| 생성한 xlsx를 SheetJS로 왕복시켜도 그대로다 |
| 깨진 zip은 «손상» 오류를 낸다 |
| 빈 파일을 거부한다 |
| 정체불명 바이트는 «명렬표 0개» → 수동 매핑으로 넘어간다 |
| BOM 붙은 UTF-8 CSV를 읽는다 |

### `partition.test.ts` — 19개

| 확인하는 것 |
| --- |
| **25명 6모둠 = `[5,4,4,4,4,4]`** |
| **25명 7모둠 = `[4,4,4,4,3,3,3]`**, 인원 차이 1 이하 |
| 31명 8모둠 = `[4,4,4,4,4,4,4,3]` |
| **1~40명 × 가능한 모든 모둠 수**에서 합계가 맞고 차이가 1 이하 |
| 학생보다 모둠이 많으면 «5개 이하로 줄여 주세요» 안내와 함께 거부 |
| 모둠 수 0·음수 거부 |
| 25명 4인 모둠 요청 시 6모둠과 7모둠이 대안에 모두 나온다 |
| 그중 6모둠이 1순위 (총 편차 기준) |
| 정확히 나누어떨어지면 그렇게 말한다 |
| 최소 > 최대면 거부 |
| 불가능하면 무한 반복 대신 오류 |
| 1명짜리 학급도 처리 |
| 짝수는 정확히 짝지어진다 |
| 홀수는 «혼자 / 3인 / 직접 지정» 어느 방식으로도 오류 없이 처리 |
| 3인 방식의 인원 합이 맞는다 |
| 같은 시드는 같은 난수열 |
| 다른 시드는 다른 난수열 |
| 셔플이 입력을 변형하지 않고 재현 가능 |
| 재시작마다 다른 자식 시드 |

### `solver.test.ts` — 31개

| 확인하는 것 |
| --- |
| 모든 배치 대상 학생이 정확히 한 자리씩 |
| **같은 시드 → 완전히 같은 결과** |
| 다른 시드 → 다른 결과 |
| 고정석이 지켜진다 (하나·둘 동시) |
| 필수 분리 조건이 지켜진다 (대각선 포함) |
| **전출·자퇴·결석 학생이 자리를 받지 않고, 나머지는 정상** |
| 좌석이 남으면 빈자리로 남는다 |
| 잠근 자리를 유지한 채 나머지만 재배치 |
| **성별 정보 없이 남녀 조건을 켜도 오류가 없다** |
| 성별이 있으면 같은 성별 짝이 기준선보다 실제로 줄어든다 |
| 과거 짝 회피를 켜면 재발이 0건 |
| 학생 0명·1명에서 예외가 없다 |
| **불가능한 조건에서 5초 안에 끝나고 `unsatisfiable` 을 반환** |
| 좌석 부족을 사전에 «19석이 모자랍니다»로 알린다 |
| 고정석 충돌을 사전에 검출 |
| «가까이»와 «떨어뜨리기» 충돌을 검출 |
| 성별 전원 미지정이면 «효과가 없습니다»라고 말한다 |
| 시험 간격이 불가능하면 시도하지 않는다 |
| 25명 6모둠 = `[5,4,4,4,4,4]`, 전원 중복 없이 배정 |
| 25명 7모둠 인원 차이 1 이하 |
| **제외 학생을 뺀 22명이 다시 균등 분배된다** |
| 고정 모둠이 지켜진다 |
| 같은 모둠 금지가 지켜진다 |
| 모둠 편성도 시드 재현 가능 |
| 지난 모둠원 재회가 줄어든다 |
| 모둠 정원 초과 고정을 사전에 검출 |
| 없는 모둠 번호 고정을 검출 |
| «리더가 3명인데 모둠은 6개»를 알려 준다 |
| 필수 위반이 조용히 통과하지 않는다 |
| 비활성화한 조건은 무시된다 |

### `layout.test.ts` — 20개

| 확인하는 것 |
| --- |
| 학생 관점은 좌표를 바꾸지 않는다 |
| **교사 관점은 정확히 180° 회전** — `(0,0) → (4,5)` |
| 변환이 자기 자신의 역함수 (전 좌표 왕복 확인) |
| **두 관점의 좌석 순서가 정확히 서로의 역순이고 좌석 집합은 동일** |
| 칠판이 학생 관점은 위, 교사 관점은 아래 |
| 교탁과 창가 방향도 교사 관점에서 좌우 반전 |
| 앞줄·뒷줄·창가·복도 영역 태그 |
| 통로 열이 좌석이 아닌 빈칸이 된다 |
| 2인 책상 묶음, 단 통로를 건너 묶지 않는다 |
| 줄·칸 추가 시 좌석 ID 중복 없음 |
| 말도 안 되는 크기(0, 999)를 안전하게 자른다 |
| 인접 관계(직교 4·대각 4, 모서리는 2·1) |
| 거리 계산 |
| 짝꿍 관계의 대칭성 |

### `history.test.ts` — 21개

| 확인하는 것 |
| --- |
| 현재 배치에서 짝·인접·모둠을 기록으로 뽑는다 |
| **기록에 이름을 넣지 않는다** (명시적으로 요청할 때만) |
| 같은 짝·같은 모둠 횟수 집계 |
| 최근 기록에 더 큰 가중치 (1 + 0.5 = 1.5) |
| «최근 N회만» 제한이 동작 |
| 뒷자리 배치 빈도(공정성) 점수 |
| 같은 프로젝트 안에서는 UUID로 연결 |
| 다시 가져오면 출석번호로 연결 |
| **동명이인은 이름으로 연결하지 않고 교사에게 넘긴다** |
| 매핑으로 기록의 ID를 다시 쓴다 |
| 교사용 백업이 모든 정보를 왕복한다 |
| **학생용 백업을 복원하면 성별이 «추정»이 아니라 «미지정»** |
| 다른 프로그램의 JSON을 거부한다 |
| 더 새로운 스키마 버전을 안내와 함께 거부한다 |
| 내보낸 엑셀의 `과거배치용데이터` 시트를 다시 읽는다 |
| 해당 시트가 비면 null |

### `privacy.test.ts` — 15개

이 파일이 개인정보 주장을 **기계적으로** 검증합니다.

| 확인하는 것 |
| --- |
| `src/` 전체에 `fetch(`·`XMLHttpRequest`·`sendBeacon`·`WebSocket`·`EventSource` 가 없다 |
| 주석을 포함해 외부 `http(s)://` 주소가 없다 |
| `@font-face`·`@import url()` 가 없다 (외부 글꼴 요청 차단) |
| **`core/` 가 React·`document`·`window`·`localStorage` 를 쓰지 않는다** |
| `console.*` 직접 호출이 없다 (프로덕션에서 침묵하는 로그 모듈만 사용) |
| 학생용 내보내기에서 교사 메모·배려 사항·성별·태그·제외 사유가 제거된다 |
| 교사가 켜면 포함된다 |
| 학생용에서 모둠 역할과 조건 목록이 제거된다 |
| **조건의 자유 메모는 교사용에서도 제거된다** |
| 학생용 JSON에 금지 필드가 하나도 없다 |
| 학생용 엑셀에 교사 열이 없다 |
| 엑셀에 약속한 5개 시트가 모두 생성된다 |

첫 다섯 항목은 **회귀 방지 장치**입니다. 몇 달 뒤 누군가 분석 도구나 CDN 링크를
무심코 추가하면 이 테스트가 즉시 실패합니다.

> 이 테스트가 실제로 자기 자신을 잡은 적이 있습니다. `src/index.css` 주석에
> "No `@font-face` rule exists anywhere"라고 써 두었더니 글꼴 검사가 걸렸습니다.
> 지금은 주석을 제거하고 검사합니다.

---

## E2E 테스트 (12개)

프로덕션 빌드를 대상으로 실행합니다. 개발 서버가 아니라 **실제 사용자가 받는 것과 같은
CSP와 서비스워커**를 검증하기 위해서입니다.

| 흐름 |
| --- |
| **나이스 엑셀 업로드** → 헤더 9행 인식 → 25명 확인 |
| **전출 처리** → 24명 → 6모둠이 `[4,4,4,4,4,4]` 로 재계산 → 되돌리면 다시 `[5,4,4,4,4,4]` |
| 25명 6모둠 + 자리 배치 → 필수 조건 전부 충족, 모둠 인원 `[5,4,4,4,4,4]` |
| **관점 전환** → 좌석 순서가 정확히 역순, 학생 집합은 동일 |
| **공개 모드** → 전체 숨김 상태에서 화면에 이름이 없음 → 한 명 공개 → 전체 공개 |
| **자리 잠금** → 시드를 바꿔 다시 만들어도 잠근 자리 유지 |
| 두 자리 직접 맞바꾸기 |
| **PNG·JSON 저장** → 파일명에 학생 이름 없음 |
| **학생용 백업에 교사 메모 없음**, 교사용으로 켜면 포함 |
| **모든 정보 삭제** → IndexedDB가 실제로 사라졌는지 브라우저에서 직접 확인 |
| 새로고침해도 명단이 자동 복구되지 않음 |
| **전 과정에서 외부 네트워크 요청 0건** |

마지막 항목은 `page.on('request')` 로 브라우저가 발생시킨 **모든** 요청을 가로채
동일 출처·`data:`·`blob:` 이 아닌 것이 하나라도 있으면 실패합니다.

---

## 테스트가 실제로 잡아낸 버그

이 목록이 테스트의 존재 이유입니다.

### 1. «모든 정보 삭제»가 데이터베이스를 되살리고 있었다

삭제 후 «정말 지워졌는지» 확인하려고 `listProjects()` 를 호출했는데,
그 안의 `indexedDB.open()` 이 **없는 데이터베이스를 새로 만들어 버렸습니다.**
결과적으로 «삭제 완료»라고 보고하면서 빈 데이터베이스를 남기고 있었습니다.

E2E가 브라우저에서 `indexedDB.databases()` 를 직접 조회해 잡았습니다.
지금은 열지 않고 존재 여부만 확인하며, 삭제 후 저장 기능을 «꺼짐»으로 되돌려
설정 패널이 데이터베이스를 다시 만들지 않게 합니다.

### 2. 명단을 고치러 갔다 오면 모둠 설정이 사라졌다

«자리 만들기» 화면의 모둠 수·인원 설정이 화면 로컬 상태여서, 학생 한 명을 고치러
명단 화면에 다녀오면 통째로 초기화됐습니다. 스토어로 옮겼습니다.

### 3. 조건이 없으면 솔버가 메모리를 낭비했다

조건이 하나도 없으면 모든 교환의 점수 변화가 0이라 전부 채택되고,
채택될 때마다 좌석 서명 문자열(25명 기준 700자)을 만들고 후보 배열을 정렬했습니다.
수만 번 반복하면 상당한 낭비입니다. «최고점을 갱신했을 때 + 재시작이 끝났을 때»만
스냅샷을 남기도록 바꿨습니다. 실제 앱을 브라우저에서 돌려 보다가 발견했습니다.

### 4. 감점이 두 번 더해지고 있었다

성별·태그 균형 평가 함수가 감점을 반환하면서 동시에 `record()` 콜백으로도
전달해, 호출부에서 양쪽을 모두 더하고 있었습니다.

### 5. 모둠 대안 순위가 직관과 어긋났다

25명·4인 모둠에서 «최대 인원 차이»로 정렬하니 5모둠(5명씩, 차이 0)이 1위였습니다.
인원은 완벽히 고르지만 **모든 모둠이 목표를 빗나간** 안입니다.
«목표와의 총 편차»로 바꾸니 6모둠(빗나간 학생 1명)이 1위가 되어,
손으로 계산한 답과 일치하게 됐습니다.

### 6. 손상 파일 처리 가정이 틀렸다

SheetJS가 임의 바이트에 예외를 던질 것으로 가정했는데, 실제로는 텍스트로
관대하게 파싱해 빈 워크북을 돌려줍니다. 그래서 «깨진 zip은 오류»와
«정체불명 바이트는 명렬표 0개 → 수동 매핑» 두 경로로 나눴습니다.

---

## 실제 파일로 확인하기

합성 픽스처는 구조만 재현하므로, 실제 파일에 대해서는 별도 점검 도구가 있습니다.

```bash
npm run inspect -- "C:/경로/1-1반 명렬표.xlsx"
```

헤더 위치·열 매핑·메타·학생 수·경고를 출력하며,
**학생 이름은 글자 수만 표시**하므로 결과를 그대로 공유해도 안전합니다.

개발 중 실제 나이스 파일 4종(같은 학교의 3개 학급 + 한셀로 저장된 다른 양식 1개)에
대해 전부 인식되는 것을 확인했습니다. 한셀 파일은 헤더가 2행, 번호·이름이 A·B열,
메타 정보가 없는 완전히 다른 구조였고 Python openpyxl로는 열리지도 않았지만,
점수 기반 탐색이 그대로 처리했습니다.

---

## 직접 확인할 수 있는 수동 점검표

### 개인정보

- [ ] F12 → Network 를 켜고 엑셀을 올린다. 앱 자신의 파일 외 요청이 없는지 확인
- [ ] 자리를 여러 번 다시 만든다. 여전히 요청이 없는지 확인
- [ ] PNG·PDF·엑셀을 저장한다. 업로드 요청이 없는지 확인
- [ ] 인터넷을 끊고 새로고침한다. 모든 기능이 동작하는지 확인
- [ ] F12 → Console 에 학생 이름이 출력되지 않는지 확인
- [ ] 주소창에 학생 이름이나 번호가 없는지 확인
- [ ] 저장을 켜지 않은 채 새로고침하면 명단이 사라지는지 확인
- [ ] 설정 → 모든 정보 삭제 후 F12 → Application → IndexedDB 가 비었는지 확인
- [ ] 학생용 PDF·엑셀에 교사 메모가 없는지 확인
- [ ] 저장한 파일 이름에 학생 이름이 없는지 확인

### 엑셀

- [ ] 우리 학교 명렬표가 그대로 인식되는지
- [ ] 학년·반·교사명이 맞게 표시되는지
- [ ] 학생 수가 실제와 같은지
- [ ] 동명이인이 두 명으로 표시되는지
- [ ] 인식이 안 되면 «직접 지정하기»로 읽히는지

### 배치

- [ ] 전출 학생을 «전출»로 바꾸면 자리와 모둠에서 빠지는지
- [ ] 그때 모둠 인원이 다시 계산되는지
- [ ] «되돌리기»로 복귀하는지
- [ ] 결석 학생이 돌아왔을 때 원래 자리로 돌아가는지
- [ ] 고정석이 여러 번 다시 만들어도 유지되는지
- [ ] 같은 시드를 넣으면 같은 배치가 나오는지
- [ ] 25명 6모둠이 5·4·4·4·4·4 로 나오는지
- [ ] 좌석이 부족하면 «만들기»가 막히고 이유가 나오는지

### 화면

- [ ] 교사 관점이 교탁에서 본 모습(교탁 아래, 좌우 반전)인지
- [ ] 학생 관점이 칠판을 바라본 모습(칠판 위)인지
- [ ] 관점을 바꿔도 학생 배치 자체는 그대로인지
- [ ] 공개 모드에서 이름이 가려지고 클릭하면 나오는지
- [ ] 발표용 크게 보기가 프로젝터에서 읽히는지
- [ ] 인쇄 미리보기에 버튼이 안 나오고 책상이 잘리지 않는지
- [ ] 긴 이름(4~5자)이 카드를 깨뜨리지 않는지
- [ ] Ctrl+Z / Ctrl+Y 가 동작하는지

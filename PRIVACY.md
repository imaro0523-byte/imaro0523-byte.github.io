# 개인정보 보호

이 문서는 이 도구가 학생 정보를 어떻게 다루는지 구체적으로 설명합니다.
"안전합니다"라는 말 대신, **무엇이 어디에 있고 왜 밖으로 나갈 수 없는지**를 적었습니다.

---

## 한 줄 요약

**학생 이름·번호·성별·교사 메모·자리 배치 결과는 어떤 서버로도 전송되지 않습니다.**
이 앱에는 백엔드도 데이터베이스도 없고, 통신 수단 자체가 브라우저 차원에서 막혀 있습니다.

이 문서의 주장은 **실제로 측정한 것**입니다. 감사 과정과 남은 한계는
[SECURITY_AUDIT.md](SECURITY_AUDIT.md)에 전부 적어 두었습니다.

---

## 어떻게 보장하나 — 5겹

### 1겹. 보낼 서버가 없다

이 프로젝트에는 서버 코드가 존재하지 않습니다. 빌드 결과물은 HTML·CSS·JavaScript 정적 파일이
전부이고, 웹 서버는 그 파일들을 내려 주는 일만 합니다. 교사가 올린 엑셀은 브라우저의
`File.arrayBuffer()` 로 읽혀 그 자리에서 파싱되고, 어디로도 올라가지 않습니다.

### 2겹. 브라우저가 통신을 막는다

`index.html`에 다음 Content-Security-Policy가 들어 있습니다.

```
default-src 'none'; base-uri 'none'; form-action 'none'; object-src 'none';
frame-src 'none'; child-src 'none'; media-src 'none'; font-src 'none';
img-src data: blob:; style-src 'self' 'unsafe-inline'; script-src 'self';
worker-src 'self' blob:; manifest-src 'self'; connect-src 'none'
```

**기본이 «전부 금지»**이고 앱이 실제로 필요한 것만 열어 둡니다.

- `connect-src 'none'` — fetch·XHR·sendBeacon·EventSource·WebSocket이 **전부** 막힙니다.
  자기 자신에게도 못 보냅니다
- `img-src data: blob:` — `'self'`가 **없습니다.** 이미지 요청으로 주소창에 데이터를
  실어 보내는 수법을 막기 위해서이며, 그래서 파비콘은 `data:` URI로 인라인했습니다
- `default-src 'none'` — iframe·미디어·글꼴 등 위에 적히지 않은 모든 것

#### 이 문서가 전에 잘못 말했던 것

초판에는 이렇게 적혀 있었습니다.

> ~~「connect-src 'self'가 핵심입니다. 이것이 가장 강한 보증입니다.」~~

**틀린 말이었습니다.** `'self'`는 「아무 데도 아님」이 아니라 「이 앱이 올라가 있는
웹 서버」입니다. 감사에서 15개 경로에 표식을 실어 실제로 쏘아 본 결과
**8개가 서버에 도달했습니다.** `new Image().src = '/?d=' + 이름들` 한 줄이면
학급 명단이 호스팅 서버의 접근 로그에 남았습니다. 교차 출처는 막고 있었지만,
동일 출처는 열려 있었습니다.

지금은 같은 시험에서 **8건 중 7건이 차단**됩니다.

### 3겹. 남은 한 구멍 — 숨기지 않고 적습니다

`<link rel="prefetch">` **하나는 지금의 Chrome이 어떤 CSP로도 막지 못합니다.**
`prefetch-src` 지시문이 제거되었고 `default-src`가 대신 적용되지도 않습니다.

- 교차 출처 prefetch는 차단됩니다 (실측 확인)
- 따라서 **제3자에게는 새지 않고**, 이 앱이 올라간 서버의 접근 로그까지만 갈 수 있습니다

브라우저가 못 막으므로 코드 검사로 막습니다. `npm test`의 소스 스캔이 `src/`
어디에서든 `rel="prefetch"`를 발견하면 실패합니다.

**CSP가 원리상 막지 못하는 것도 있습니다.** 주소를 바꿔 버리는 방식
(`location.href = '...'`, 사용자 클릭에 반응하는 `window.open`)은 어떤 지시문으로도
차단할 수 없습니다. 이 앱에 그런 코드가 없다는 것만이 방어이며, 소스 스캔이
그 세 가지를 함께 검사합니다.

### 4겹. 코드에 통신 수단이 없다

`npm test`를 실행하면 `tests/unit/privacy.test.ts`가 `src/` 전체를 훑어
**15가지 패턴** 중 하나라도 있으면 테스트를 실패시킵니다.

`fetch(` · `XMLHttpRequest` · `sendBeacon` · `WebSocket` · `EventSource` ·
`navigator.share` · `ReportingObserver` · `importScripts` · `window.open` ·
`location.href/assign/replace` · `form.submit()` · `new Image()` ·
`createElement('img'|'script'|'iframe'|'link'|'form'|'object'|'embed')` ·
`rel="prefetch|preconnect|dns-prefetch|preload"` · 외부 `http(s)://` 주소 ·
`@font-face` · `console.*` 직접 호출

초판은 이 중 4가지만 검사했습니다. 이미지 `src`나 CSS `url()`도 서버에 데이터를
보내는 데 똑같이 쓸 수 있는데 아무도 보고 있지 않았습니다.

앱이 만드는 DOM 요소가 **`<a download href="blob:…">` 하나뿐**임을 별도 테스트로
고정해, 다른 요소가 몰래 늘어나지 않게 합니다.

### 5겹. 실제로 요청이 나가는지 측정한다

`npm run test:e2e`가 프로덕션 빌드를 상대로:

- 전체 작업 흐름(업로드 → 편집 → 모둠 → 배치 → PNG·PDF·엑셀·JSON)을 수행하며
  **서비스워커가 낸 것을 포함해 모든 요청**을 기록하고, 앱 자신의 정적 파일이 아닌
  요청이 하나라도 있으면 실패합니다
- 학생 필드마다 **서로 다른 표식**을 넣고, 요청 URL·쿼리스트링·본문에 그 값이
  나타나면 실패합니다
- 17개 유출 경로에 표식을 실어 **실제로 쏘아 보고** 각 요청의 결말을 확인합니다

> 측정에 함정이 하나 있었습니다. 요청 이벤트가 발생했다고 해서 바이트가 나간 것은
> 아닙니다. Chrome은 요청 이벤트를 올린 뒤 CSP로 실패시킵니다. 그래서 도구는
> 「전달됨」과 「차단됨(사유)」을 구분합니다. 이 구분이 없으면 차단된 교차 출처
> 요청을 유출로 오판합니다.

### CSP 전달 방식의 한계

CSP는 **meta 태그로만** 전달됩니다. GitHub Pages가 사용자 지정 응답 헤더를 지원하지
않기 때문입니다. 여기서 오는 실질적 차이가 하나 있습니다.

**`frame-ancestors`는 meta 태그에서 명세상 무시됩니다.** 그래서 GitHub Pages에
올린 경우 **악성 사이트가 이 앱을 iframe으로 감쌀 수 있습니다.** 앱이 하는 일은
여전히 이 컴퓨터 안에서만 일어나지만, 감싼 화면을 꾸며 사람을 속이는 데 쓰일 수는
있습니다.

초판에는 정책 문자열에 `frame-ancestors 'none'`이 들어 있었습니다. 아무 효력도
없으면서 보호받는 것처럼 보였으므로 **meta에서 뺐습니다.** 대신
`public/_headers`에 넣어 두었으니, Netlify·Cloudflare Pages처럼 헤더를 보낼 수 있는
곳에 올리면 자동으로 적용됩니다.

**서비스워커는 자기만의 CSP를 가집니다.** 문서의 meta CSP를 상속하지 않으므로
정적 호스트에서는 사실상 정책이 없는 상태로 동작합니다. 이 앱의 서비스워커는
빌드 산출물이고 앱 셸만 프리캐시하지만, 사실 자체를 적어 둡니다.

## 무엇이 어디에 저장되나

### 기본값: 아무 데도 저장하지 않음

앱을 처음 열면 **저장 기능이 꺼져 있습니다.** 학생 정보는 브라우저 메모리에만 존재하며,
탭을 닫거나 새로고침하면 사라집니다. 여러 선생님이 함께 쓰는 교무실 컴퓨터에서도
다음 사람이 앞사람의 명렬표를 볼 수 없습니다.

### 저장을 켰을 때

**설정 → «이 브라우저에 저장하는 기능 켜기»** 를 체크하고 **«지금 로컬에 저장»** 버튼을
직접 눌렀을 때만 저장됩니다. 자동 저장은 없습니다.

| 저장 위치 | 저장되는 것 |
| --- | --- |
| **IndexedDB** (`seat-planner` 데이터베이스) | 학생 명단(이름·번호·성별·태그·교사 메모·배려 사항), 교실 구조, 자리 배치, 모둠 편성, 조건, 과거 기록, 랜덤 시드 |
| **localStorage** (`seatPlanner.settings.v1`) | 화면 밝기, 보기 관점, 애니메이션 줄이기, 이름/번호 표시 여부, 저장 기능 켬 여부 — **학생 정보는 들어가지 않습니다** |

이 데이터는 **그 컴퓨터의 그 브라우저 안에만** 있습니다. 다른 기기, 다른 브라우저,
같은 브라우저의 시크릿 창에서는 보이지 않습니다. 클라우드 동기화도 되지 않습니다.

---

## 완전히 지우는 방법

**설정 → «모든 정보 삭제»** 버튼을 누르면 다음을 순서대로 수행합니다.

1. IndexedDB의 `seat-planner` 데이터베이스 삭제
2. `seatPlanner.` 로 시작하는 localStorage·sessionStorage 항목 삭제
3. **이 앱의 캐시만** 삭제 (`workbox-`·`seat-planner` 접두사)
4. 서비스워커 등록 해제
5. 화면의 명단·배치·기록을 메모리에서 비우고, 저장 기능을 다시 «꺼짐»으로 되돌림
6. **삭제 후 재확인** — 실제로 사라졌는지 다시 조회해 결과를 화면에 표시

### 이 절차가 두 번 고쳐진 이유

**첫 번째.** 확인하려고 데이터베이스를 다시 열었는데, `indexedDB.open()`은 없는
데이터베이스를 **새로 만들어 버립니다.** 「삭제했다」고 보고하면서 빈 데이터베이스를
남기고 있었습니다. 지금은 `indexedDB.databases()`로 존재 여부만 봅니다.

**두 번째 (감사에서 발견).**

- **캐시를 전부 지우고 있었습니다.** `user.github.io`처럼 한 도메인을 여러 프로젝트가
  나눠 쓰는 경우 **남의 앱 캐시까지** 지웠습니다. 지금은 이 앱 것만 지우고, 건드리지
  않은 개수를 화면에 표시합니다
- **확인할 수 없는데 확인했다고 말했습니다.** Firefox에는 `indexedDB.databases()`가
  없습니다. 그런데도 「확인 완료」로 표시했습니다. 지금은 세 가지를 구분합니다 —
  「확인 완료」 · 「남아 있음」 · **「삭제했지만 이 브라우저에서는 확인하지 못했습니다」**
- **삭제가 막혔는데 성공으로 처리했습니다.** 다른 탭이 열려 있으면 삭제가 `blocked`
  되는데 그냥 진행했습니다. 지금은 「다른 탭을 닫고 다시 눌러 주세요」라고 말합니다
- 데이터베이스 연결이 오류 경로에서 닫히지 않아 삭제를 막을 수 있었습니다. 지금은
  모든 경로에서 닫습니다

> **주의:** 이미 내보낸 PNG·PDF·Excel·JSON 파일은 이 버튼으로 지워지지 않습니다.
> 사용자의 다운로드 폴더에 있으므로 직접 삭제해 주세요.

---

## 서비스워커(오프라인 캐시)

**기본값은 꺼짐입니다.** 설정 → «오프라인으로 쓸 수 있게 하기»를 직접 켰을 때만
서비스워커가 등록됩니다.

- 캐시 대상: 빌드된 앱 파일(`index.html`, `assets/*.js`, `assets/*.css`, 아이콘, 매니페스트)
- 런타임 캐싱: **없음** (`runtimeCaching: []`)
- **업로드한 엑셀 원본과 학생 데이터는 캐시되지 않습니다.** 이 앱은 학생 데이터를
  네트워크 요청으로 다루지 않으므로 서비스워커가 가로챌 대상 자체가 없습니다
- 끄면 등록이 해제되고 캐시도 함께 지워집니다

> **감사에서 발견된 것:** 이전 버전은 서비스워커를 **만들기만 하고 등록하지 않았습니다.**
> 등록 코드가 아예 존재하지 않았는데 README는 오프라인 사용을 약속하고 있었습니다.
> 지금은 등록 코드가 있고, **네트워크를 끊고 새로고침해 실제로 열리는지**를 E2E 테스트가
> 확인합니다. 캐시에 학생 데이터가 없는 것도 함께 확인합니다.

---

## 내보낸 파일에 무엇이 들어가나

결과 화면의 **«교사용 정보도 함께 넣기»** 체크박스가 이를 결정하며, **기본값은 꺼짐**입니다.

| 항목 | 기본(학생용) | 켰을 때(교사용) |
| --- | --- | --- |
| 번호·이름 | 포함 | 포함 |
| 자리·모둠 | 포함 | 포함 |
| 성별 | **제외** | 포함 |
| 태그 | **제외** | 포함 |
| 교사 메모 | **제외** | 포함 |
| 배려 사항 | **제외** | 포함 |
| 제외 사유(전출·자퇴 등) | **제외** | 포함 |
| 모둠 역할 | **제외** | 포함 |
| 조건 목록 | **제외** | 포함 |
| 조건에 적은 메모 | **제외** | **제외** |

마지막 줄이 특히 중요합니다. 조건에 붙이는 자유 메모("지난 학기 다툼이 있었음" 같은)는
교사용 파일에도 넣지 않습니다. 관계에 관한 민감한 서술이 가장 남기 쉬운 곳이기 때문입니다.

이 규칙은 `src/core/exportData/redact.ts` 한 곳에 모여 있고, 내보내기 직전에
`findLeakedFields()` 가 결과물을 한 번 더 검사해 금지된 항목이 발견되면 **저장을 중단**합니다.

### 파일 이름

`자리배치_1-1_20260812.png` 처럼 학급과 날짜만 들어갑니다. **학생 이름은 파일 이름에
들어가지 않습니다.** 파일 목록만 보고 특정 학생을 식별할 수 없게 하기 위해서입니다.

---

## 화면과 로그에 남지 않게 한 것

- **프로덕션 빌드에서 콘솔 출력은 전부 비활성화**됩니다(`src/lib/log.ts`).
  게다가 로그 함수의 인자 타입이 `string | number | boolean | null | undefined` 로
  제한되어 있어, 개발 중에도 학생 객체를 실수로 로그에 넘길 수 없습니다.
- **오류 화면에 데이터를 뿌리지 않습니다.** React 에러 바운더리는 컴포넌트 스택과 props를
  버리고 짧은 안내만 표시합니다. 프로젝터에 명단이 통째로 뜨는 사고를 막기 위해서입니다.
- **URL에 학생 정보를 넣지 않습니다.** 쿼리스트링·해시에 이름이나 번호가 실리지 않으므로,
  주소창을 캡처해 공유해도 안전합니다.
- **소스맵을 만들지 않습니다**(`sourcemap: false`).
- **학생 공개 모드**에서는 교사 메모·조건·성별·태그가 화면에 존재하지 않습니다.
  처음 상태가 «전부 숨김»이라, 수업 중 새로고침으로 결과가 의도치 않게 공개되지 않습니다.

---

## 이 저장소와 학생 데이터

- `.gitignore` 가 `*.xlsx`, `*.xls`, `*.csv` 를 제외하므로 실제 명렬표가 커밋될 수 없습니다.
- 테스트 픽스처는 `학생01`~`학생25` 같은 **합성 데이터**를 코드로 생성합니다.
  실제 학생 이름은 코드·테스트·문서·스냅샷 어디에도 없습니다.
- Playwright의 스크린샷·비디오·트레이스 기록을 꺼 두었습니다. 실패한 테스트의 아티팩트에
  화면 속 명단이 담기는 것을 막기 위해서입니다.

---

## 솔직하게 밝혀 두는 것

- **번들에 `cdnjs.cloudflare.com` 문자열이 들어 있습니다.** jsPDF의
  `output('pdfobjectnewwindow')` 모드가 PDF 미리보기용 외부 스크립트를 삽입하는 코드입니다.
  이 앱은 `save()` 만 호출하므로 **도달하지 않는 코드**이고, 설령 실행되더라도
  `script-src 'self'` 가 외부 스크립트 로드를 차단합니다. 그래도 파일 안에 문자열이
  존재하는 것은 사실이므로 적어 둡니다.
- **`<link rel="prefetch">` 한 경로는 막지 못합니다.** Chrome에 이를 통제할 CSP 지시문이
  없습니다. 교차 출처는 차단되므로 제3자에게 새지는 않고, 호스팅 서버의 접근 로그까지만
  갈 수 있습니다. 코드 검사로 대신 막습니다.
- **주소 이동(navigation)은 CSP가 막을 수 없습니다.** `location.href`나 사용자 클릭에
  반응하는 `window.open`은 어떤 지시문으로도 차단되지 않습니다. 이 앱에 그런 코드가
  없다는 것이 유일한 방어이며, 소스 스캔이 검사합니다.
- **GitHub Pages에서는 iframe 감싸기를 막지 못합니다.** `frame-ancestors`가 meta 태그에서
  무시되고 GitHub Pages는 헤더를 보낼 수 없기 때문입니다.
- **모든 측정은 Chromium에서만 했습니다.** Firefox·Safari의 동작은 확인하지 않았습니다.
- **업로드 파일에는 상한이 있습니다.** 20MB·시트당 20,000행·512열·100시트를 넘으면
  열지 않고 이유를 알려 줍니다. 잘못된 파일로 탭이 멈추는 것을 막기 위해서입니다.
- **내보낸 파일은 암호화되지 않습니다.** 학생 이름이 평문으로 들어 있으므로 저장 위치와
  전달 경로는 사용자가 관리해야 합니다. 앱은 저장 직전에 이 사실을 화면에 안내합니다.
- **브라우저 자체의 기능은 이 앱이 통제할 수 없습니다.** 확장 프로그램, 기업용 감시 정책,
  브라우저 동기화 설정 등은 앱 밖의 영역입니다.

---

## 직접 확인하는 방법

1. 앱을 열고 **F12 → Network 탭**을 켭니다.
2. 명렬표를 올리고 자리를 만들고 PNG로 저장합니다.
3. 앱 자신의 파일(`index.html`, `.js`, `.css`) 외에 어떤 요청도 없어야 합니다.
4. **인터넷 연결을 끊고** 새로고침해 보세요. 모든 기능이 그대로 동작합니다.

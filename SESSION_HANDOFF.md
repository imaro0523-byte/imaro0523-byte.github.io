# 세션 인수인계

> 이 문서는 대화 기억이 아니라 **실제 git 상태와 실행한 명령의 출력**을 근거로 작성했습니다.
> 확인하지 못한 것은 「미확인」으로 표시했습니다.

---

## 1. 세션 기본 정보

| 항목 | 값 |
| --- | --- |
| 작성 시각 | 2026-08-13 (KST) |
| 프로젝트 | 자리배치 도우미 (Seat Planner) — 교실 자리·모둠 배치 웹앱 |
| 저장소 경로 | `C:\Users\pc\dev\seat-planner` |
| 현재 브랜치 | `master` |
| 최신 commit hash | `4331f675b05c3c86dc1f764b6044298016261eca` (`4331f67`) |
| 최신 커밋 제목 | 공개 배포 준비: 소개 사이트 분리, 피드백 창구, 광고 자리 |
| working tree | **clean** (`git status --porcelain` 출력 없음) |
| 총 커밋 수 | 11 |
| 원격 저장소 | **없음** (`git remote -v` 비어 있음 — 아직 GitHub에 올리지 않음) |
| 추적 파일 수 | 99 |
| 코드 규모 | `src/` 10,982줄 · `tests/` 3,931줄 |
| 프로젝트 단계 | **기능 완성 + 개인정보 감사 완료 + 배포 준비 완료. 실제 배포는 미수행** |

### 이번 세션의 주된 작업 목적

1. 나이스 명렬표 기반 자리·모둠 배치 웹앱을 처음부터 구현 (Phase 1~5)
2. 사용자 피드백에 따른 기능 수정 5건
3. 사용자가 제시한 13개 항목 개인정보 감사 수행 후 발견 사항 수정
4. 공개 배포 구조·피드백 창구·광고 자리 준비

### 프로젝트 규칙 파일

**`CLAUDE.md`, `AGENTS.md`, `.cursorrules` 는 존재하지 않습니다.** 이 저장소에는
프로젝트 규칙 파일이 없으며, 아래 「5. 반드시 유지해야 할 제약조건」이 사실상 그 역할을 합니다.

---

## 2. 이번 세션에서 완료한 작업

### 2-1. 나이스 엑셀 파서 (커밋 `2cc4611`)

| 항목 | 내용 |
| --- | --- |
| 목적 | 실제 나이스 «교과시간별출석부»를 파일 수정 없이 읽기 |
| 근거 | 실제 파일 4종의 구조를 직접 분석한 뒤 규칙 작성 (추정 아님) |
| 핵심 파일 | `src/core/excel/detectHeader.ts`, `parseRoster.ts`, `meta.ts`, `importRoster.ts`, `readWorkbook.ts` |
| 핵심 함수 | `scoreHeaderRow()` — 점수 기반 헤더 탐색, `parseNumberLike()` — `1.0`→`1` |
| 완료 근거 | 실제 파일 4종 인식 성공 (25·25·24·31명, 경고 0건) + 단위 테스트 28개 |

실제 파일에서 확인한 함정: A열이 비어 표가 B열부터 시작 · 헤더에 빈 문자열 셀 20여 개 ·
번호가 부동소수(`1.0`) · `1 / 1` 페이지 번호와 학교명 푸터.

한셀(HCell)로 저장된 파일 1종은 Python openpyxl로는 열리지 않았으나 이 파서는 읽었습니다.

### 2-2. 제약 조건 엔진과 솔버 (커밋 `d1f2829`)

| 항목 | 내용 |
| --- | --- |
| 목적 | 단순 shuffle이 아니라 조건을 지키는 배치 |
| 핵심 파일 | `src/core/constraints/{kinds,evaluate,diagnose}.ts`, `src/core/solver/{rng,partition,seating,grouping}.ts` |
| 핵심 값 | `SEVERITY_WEIGHT = { hard: 100000, strong: 200, weak: 20 }` |
| 예산 | `EFFORT_BUDGETS` — fast 150ms / balanced 800ms / thorough 3000ms |
| 완료 근거 | 단위 테스트 31개 (고정석·필수분리·시드재현·시간예산 종료 포함) |

### 2-3. 화면·내보내기·문서 (커밋 `eb90246`)

6단계 화면, PNG·JPG·PDF·Excel·JSON·인쇄, Undo/Redo, 문서 6종.

### 2-4. 사용자 피드백 반영 5건

| 커밋 | 문제 | 조치 | 완료 근거 |
| --- | --- | --- | --- |
| `3ad15e8` | 모둠원이 교실 전체에 흩어짐 | 감점 대신 **기하학**으로 해결 — 모둠마다 책상 섬 생성, `groupSlot` 새김, 솔버는 같은 섬 안에서만 교환 | 단위 18개 + E2E 1개. 모든 시드에서 학생이 자기 섬 안에 앉음 |
| `15c8f1f` | 교탁 위치 설정이 화면에 반영 안 됨 | 원인은 `flex-1`이 여백을 흡수해 `justifyContent`가 무효. 고치는 대신 **설정 제거** (배치 결과에 영향 없는 값) | typecheck + 전체 테스트 |
| `20e37f9` | 성별 25명 일일이 입력 | 이름 순서가 되돌아가는 지점으로 **구분1/구분2 자동 분할** | 실제 파일 3종에서 1순위가 진짜 경계 (12/13, 12/13, 11/13). 단위 24개 + E2E 2개 |
| `f1fed20` | 「모둠 6개」 템플릿이 12명씩 두 덩어리로 보임 | 가짜 템플릿 제거, 진짜 섬 생성기로 교체. 4~9모둠 선택 | E2E 1개 (6모둠→섬 6개, 8모둠→섬 8개) |
| `c413ffe` | 자리 바꾸면 이전 모둠 색 유지 | `regroupFromSeats()` — 섬 교실에서 자리가 곧 모둠 | 단위 6개 + E2E 1개 |
| `11c0044` | 5명 모둠 위치 고정 | `arrangeSizes()` — 큰 모둠을 원하는 섬으로 | 단위 10개 + E2E 1개 |

### 2-5. 개인정보 감사와 수정 (커밋 `b75feb5`)

**감사 결과 FAIL 4건 발견.** 상세는 `SECURITY_AUDIT.md`.

| 발견 | 실측 근거 | 조치 |
| --- | --- | --- |
| **동일 출처로 학생 데이터 유출 가능** | 15개 경로에 표식 실어 실측 → **8건이 서버 도달** | CSP를 `default-src 'none'` 기반으로 재작성. 8→1건 |
| **서비스워커가 등록되지 않음** | `src`·`dist` 어디에도 `serviceWorker` 문자열 없음 | `src/lib/pwa.ts` 생성, 설정에 명시적 스위치 |
| **`frame-ancestors`가 meta에서 무시됨** | CSP 명세 | meta에서 제거, `site/_headers`로 이전 |
| **삭제 검증 결함** | `indexedDB.databases()` 미지원 시 거짓 「확인 완료」 | `deleted`/`blocked`/`failed` 구분 + 「확인 불가」 상태 신설 |
| 캐시 전체 삭제 | 코드 확인 | `workbox-`·`seat-planner` 접두사만 삭제 |
| 미사용 의존성 6개 | import 0건 확인 | 제거 (`@dnd-kit`×2, `@radix-ui`×4) |
| 업로드 제한 없음 | 코드 확인 | 20MB·20,000행·512열·100시트 |

### 2-6. 배포 준비 (커밋 `4331f67`)

소개 사이트와 앱 분리, 「의견 보내기」 + 「이름 가린 화면 저장」, `DEPLOY.md`.

---

## 3. 현재 프로젝트 상태

### 지금 실행 가능한 것

| 명령 | 결과 |
| --- | --- |
| `npm run dev` | 개발 서버 (localhost:5173) |
| `npm run build` | 배포 폴더 조립 → `dist/`(소개) + `dist/app/`(앱) |
| `npm run build:app` | 앱만 → `dist/` |
| `npm test` | 단위 192개 통과 |
| `npm run test:e2e` | E2E 28개 통과 |
| `npm run typecheck` | 오류 0 |
| `npx tsx scripts/inspect-roster.ts "<경로>"` | 실제 명렬표 인식 점검 (이름 미출력) |

### 아직 실행할 수 없는 것

| 항목 | 이유 |
| --- | --- |
| **공개 배포** | git 원격 저장소가 없습니다. `git remote add` + `push` 미수행 |
| **피드백 수집** | `src/config/links.ts`의 `FEEDBACK_FORM = ''` — 구글 폼 주소 미설정 |
| **광고 노출** | `site/index.html`의 애드센스 스크립트가 주석 처리됨 (`ca-pub-XXXX` 자리표시자) |
| `npm run inspect` | **package.json에 스크립트가 없습니다** (문서는 이 명령을 안내 중 — 아래 8-1) |
| `npm run fixtures` | **`scripts/make-fixtures.ts`가 존재하지 않습니다** (아래 8-2) |

### 이 프로젝트에 해당하지 않는 상태 구분

인수인계 요청서의 «연구용 / paper-only / live-ready», «실거래 동작 생성 여부» 같은 항목은
**금융·트레이딩 시스템용 분류이며 이 프로젝트에는 해당 개념이 존재하지 않습니다.**
이 저장소에는 주문·체결·시세·백테스트 코드가 전혀 없습니다.

굳이 대응시키면 다음과 같습니다.

| 요청서 항목 | 이 프로젝트의 실제 값 |
| --- | --- |
| 실거래/주문 생성 가능 여부 | **불가능 — 해당 기능 자체가 없음** |
| live_ready 상당 | 앱 기능은 완성, **배포만 미수행** |
| 외부 전송 가능 여부 | **불가능에 가까움** (아래 5절, 잔여 1경로 존재) |

### 현재 활성화된 설정

| 설정 | 값 | 위치 |
| --- | --- | --- |
| CSP (프로덕션) | `default-src 'none'` … `connect-src 'none'`, `img-src data: blob:` | `vite.config.ts` `cspFor()` |
| CSP (개발) | 위 + `connect-src 'self' ws://localhost:*` (HMR용) | 같은 함수 |
| 로컬 저장 | **꺼짐** (기본값) | `DEFAULT_SETTINGS.storageEnabled = false` |
| 오프라인(서비스워커) | **꺼짐** (기본값, 사용자가 켜야 등록) | `src/lib/pwa.ts` |
| 기본 보기 관점 | `teacher` | `DEFAULT_SETTINGS.viewpoint` |
| 모둠 교실 자동 생성 | **켜짐** | `DEFAULT_GENERATE.autoGroupRoom = true` |
| 소스맵 | **끔** | `vite.config.ts` `build.sourcemap: false` |
| Playwright 스크린샷·비디오 | **끔** | `playwright.config.ts` |

### 비활성 / 자리표시자 상태

| 항목 | 현재 값 | 비고 |
| --- | --- | --- |
| `FEEDBACK_FORM` | `''` | 비면 앱이 링크 대신 안내 문구 표시 (의도된 동작, E2E로 고정) |
| `PROJECT_HOME` | `''` | 비면 링크 숨김 |
| 애드센스 스크립트 | HTML 주석 안 | 주석 밖 활성 `<script>` 태그 0개 확인 |

---

## 4. 핵심 결정사항과 이유

### 4-1. `core/`에 React·DOM 금지

- **결정** `src/core/` 전체를 순수 TypeScript로 유지, 테스트로 강제
- **이유** ① Node에서 jsdom 없이 테스트 ② Web Worker에 그대로 이식 ③ UI 버그가 배치 결과를 오염시킬 수 없음
- **근거** 단위 192개가 수 초 내 완료, 솔버 워커가 별도 이식 없이 동작
- **대안** 검토 후 기각: UI와 로직 혼재 — 테스트가 느려지고 워커 이식이 불가능
- **재검토 조건** 코어에서 DOM API가 꼭 필요한 기능이 생길 때 (현재 없음)

### 4-2. 정본 좌표계는 「학생 관점」 하나

- **결정** 저장 좌표는 항상 학생 관점(row 0 = 칠판 쪽). 교사 관점은 **렌더 시점 180° 회전**
- **이유** 한 번의 회전이 상하·좌우를 동시에 뒤집어 "교사의 왼쪽 = 학생의 오른쪽"이 자동 성립. 회전이 자기 역함수라 역변환에 같은 함수 사용
- **근거** E2E가 두 관점의 좌석 순서가 정확히 서로의 역순임을 확인
- **대안** 기각: 두 좌표계를 각각 저장 — 동기화 버그의 온상
- **재검토 조건** 사각형이 아닌 교실(원형 등)을 지원할 때

### 4-3. 모둠 응집은 감점이 아니라 기하학으로

- **결정** 모둠마다 책상 섬을 만들고 좌석에 `groupSlot`을 새김. 솔버는 같은 `groupSlot`끼리만 교환
- **이유** **첫 구현이 감점 방식이었고 실제로 실패했습니다.** 감점은 다른 조건과 경쟁하다 지고, 사용자가 6모둠이 교실 전체에 흩어진 결과를 받았습니다
- **근거** 사용자 스크린샷 + 수정 후 모든 시드에서 섬 안 배치 확인
- **대안** 기각: 응집 감점 상향 — 다른 조건을 밀어내면서도 보장은 안 됨
- **재검토 조건** 교사가 섬이 아닌 자유 배치에서 모둠을 쓰려 할 때

### 4-4. 구분1/구분2 — 남/여로 이름 붙이지 않음

- **결정** 이름 순서 리셋 지점으로 두 무리를 나누되 **구분1·구분2**로만 표시
- **이유** 이 코드는 **정렬 구조**를 읽지 이름을 읽지 않습니다. 어느 무리가 남학생인지 알 방법이 없고, 학교·학기마다 순서가 바뀝니다. 모르는 것을 아는 척하면 절반은 틀립니다
- **근거** 실제 파일 3종에서 경계는 정확히 찾았으나, 어느 쪽이 남/여인지는 파일에 정보가 없음
- **대안** 기각: 첫 무리를 남학생으로 가정 — 근거 없는 추측
- **보완** 「구분1 ↔ 구분2 바꾸기」 버튼 제공
- **재검토 조건** 성별 열이 있는 명렬표 양식이 표준이 될 때

### 4-5. CSP를 `default-src 'none'`으로 (감사 후)

- **결정** 기본 전면 금지 + 필요한 것만 개방. `connect-src 'none'`, `img-src`에서 `'self'` 제거
- **이유** **초판의 `connect-src 'self'`는 실측에서 8개 경로 유출을 허용했습니다.** `'self'`는 "아무 데도 아님"이 아니라 "이 앱이 올라간 웹 서버"입니다
- **근거** `tests/e2e/privacy-audit.spec.ts` 탐침 결과 (8건 → 1건)
- **대안** 기각: `connect-src`만 `'none'` — 이미지·CSS·iframe·prefetch 4경로가 남음
- **부작용 처리** `img-src`에 `'self'`가 없어 파비콘을 `data:` URI로 인라인
- **재검토 조건** 새 브라우저 지시문(prefetch 통제 수단)이 나올 때

### 4-6. XLSX에 따옴표 접두사 방어를 넣지 않음

- **결정** `=`, `+`, `-`, `@`로 시작하는 입력에 `'` 접두사를 **붙이지 않음**
- **이유** 실측 결과 SheetJS가 문자열 셀(`t='s'`)로 기록하고 `<f>` 태그가 없어 **Excel이 평가하지 않습니다.** `.xlsx`는 CSV와 달리 셀 타입이 명시적입니다. 접두사를 붙이면 「-지각 잦음」 같은 정상 메모 앞에 따옴표가 보여 **없는 문제를 막으면서 데이터를 손상**시킵니다
- **근거** 악성 문자열 5종을 넣고 셀 타입·`<f>` 태그·왕복 파싱 확인
- **대안** 기각: 방어적으로 접두사 추가
- **재검토 조건** **CSV 내보내기를 추가하는 순간 반드시 재검토.** CSV에는 셀 타입이 없어 인젝션이 성립합니다. 회귀 테스트가 이 동작을 고정 중

### 4-7. 소개 사이트와 앱 분리 (광고)

- **결정** `dist/`(소개, 광고 O) + `dist/app/`(앱, 광고 X)
- **이유** 광고 스크립트는 같은 페이지에서 도는 남의 코드라 **화면의 학생 명단을 읽을 수 있습니다.** 감사로 막은 경로가 정확히 그것입니다
- **근거** 사용자와 합의(2026-08-13 대화). 빌드 산출물에서 앱의 `script-src 'self'` 유지 확인
- **대안** 기각: 앱에 직접 삽입 — 개인정보 문서 주장이 전부 거짓이 됨
- **부수 이점** 단일 도구 페이지는 애드센스에서 low value content로 반려되기 쉬움
- **재검토 조건** 없음. **이 결정은 뒤집지 마십시오**

### 4-8. 모둠 대안 순위 = 목표 인원과의 **총 편차**

- **결정** 「최대 인원 차이」가 아니라 「목표와의 총 편차」로 정렬
- **이유** 최대 차이로 정렬하니 25명·4인 요청에 **5모둠(5명씩, 차이 0)이 1위**가 됐습니다. 인원은 고르지만 모든 모둠이 목표를 빗나간 안입니다
- **근거** 총 편차로 바꾸니 6모둠(빗나간 학생 1명)이 1위 — 손 계산과 일치
- **재검토 조건** 교사가 「인원 균등」을 목표 인원보다 우선하고 싶어 할 때

### 4-9. SheetJS를 npm이 아닌 공식 CDN 타르볼로

- **결정** `xlsx: https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz`
- **이유** npm 레지스트리 판은 0.18.5에서 멈췄고 CVE-2023-30533·CVE-2024-22363이 있습니다
- **주의** **CI가 `cdn.sheetjs.com`에 접근 가능해야 `npm ci`가 성공합니다**
- **재검토 조건** SheetJS가 npm 배포를 재개하거나 CDN이 불안정할 때

### 4-10. 저장은 기본 꺼짐, 명시적 버튼

- **결정** 자동 저장 없음. 설정에서 켜고 「저장」을 눌러야 IndexedDB 기록
- **이유** 여러 교사가 함께 쓰는 교무실 PC에서 다음 사람이 앞사람 명렬표를 보면 안 됨
- **근거** E2E: 새로고침 후 명단 자동 복구되지 않음 확인

---

## 5. 반드시 유지해야 할 제약조건

> 인수인계 요청서의 예시(`live_trading_enabled`, `orders_generated`, D+1/D+2 체결,
> factor lookahead, long-only 등)는 **이 저장소에 존재하지 않는 개념**입니다.
> 저장소를 확인한 결과 주문·체결·시세·백테스트 코드가 없습니다.
> 아래는 **이 프로젝트에 실제로 적용된 제약**입니다.

### 5-1. 개인정보 불변식 (최우선 — 깨면 프로젝트의 존재 이유가 사라짐)

| # | 제약 | 강제 수단 |
| --- | --- | --- |
| P1 | **이름으로 성별을 추정하지 않는다** | `gender` 기본 `'unset'`. 단위 테스트가 확인 |
| P2 | **학생 키는 UUID. 이름을 키로 쓰지 않는다** | 동명이인 UUID 분리 테스트 |
| P3 | **`src/`에 어떤 유출 수단도 없다** | `tests/unit/privacy.test.ts` 15개 패턴 스캔. 위반 시 **빌드 실패** |
| P4 | **`src/`에 외부 URL 금지** — 예외는 `src/config/links.ts` 단 하나 | 같은 테스트 |
| P5 | **외부 링크에 쿼리스트링·프래그먼트 금지** | 같은 테스트 (데이터가 링크에 편승하는 것 방지) |
| P6 | **앱이 만드는 DOM 요소는 `<a download href="blob:">` 하나뿐** | 같은 테스트 |
| P7 | **프로덕션 콘솔 출력 전면 금지** | `src/lib/log.ts`가 no-op. 타입상 `Student`를 로그에 못 넘김 |
| P8 | **학생용 내보내기에서 교사 정보 제거** | `redact.ts` + 6개 필드 값 수준 테스트 |
| P9 | **조건의 자유 메모는 교사용에도 미포함** | 관계 서술이 가장 남기 쉬운 곳 |
| P10 | **파일명·URL에 학생 이름 금지** | E2E 확인 |
| P11 | **저장 기본 꺼짐 / 새로고침 시 자동 복구 없음** | E2E 확인 |
| P12 | **앱(`/app/`)에 광고·분석 스크립트 금지** | CSP `script-src 'self'` + P4 |
| P13 | **실제 명렬표를 커밋하지 않는다** | `.gitignore`가 `*.xlsx`·`*.xls`·`*.csv` 제외 |
| P14 | **테스트·문서에 실제 학생 이름 금지** | 합성 데이터 `학생01`~ 만 사용 |

### 5-2. 배치 로직 불변식

| # | 제약 | 강제 수단 |
| --- | --- | --- |
| L1 | **필수(hard) 조건을 조용히 무시하지 않는다** | 위반 후보는 폐기, 화면에 위반 내역 표시 |
| L2 | **솔버는 반드시 종료한다** (벽시계 예산) | 불가능 조건에서 5초 내 종료 테스트 |
| L3 | **같은 시드 → 같은 결과** | 재현성 테스트. `Math.random()`은 솔버에서 미사용 |
| L4 | **모둠 인원 차이 ≤ 1** | 1~40명 × 모든 모둠 수 전수 테스트 |
| L5 | **관점 전환이 배치 결과를 바꾸지 않는다** | 정본 좌표에서만 계산 |
| L6 | **섬 교실에서 좌석이 곧 모둠** | `regroupFromSeats()` |
| L7 | **배치 제외 학생은 좌석·모둠 계산에서 빠진다** | `isPlaceable()` |

### 5-3. 알려진 잔여 위험 (해소 불가, 문서화로 관리)

| # | 내용 |
| --- | --- |
| R1 | **`<link rel=prefetch>` 동일 출처 1건**은 현재 Chrome의 어떤 CSP 지시문으로도 막을 수 없음. 교차 출처는 차단됨(실측). 소스 스캔이 대신 방어 |
| R2 | **주소 이동(navigation)은 CSP 사정권 밖.** `location.href`·`window.open`은 차단 불가. 소스 스캔이 방어 |
| R3 | **GitHub Pages에서는 iframe 감싸기 차단 불가** (`frame-ancestors`가 meta에서 무시). Netlify·Cloudflare Pages는 `site/_headers`로 해결 |
| R4 | **서비스워커는 자체 CSP를 가짐** (문서 meta를 상속 안 함) |
| R5 | **모든 개인정보 측정은 Chromium 한 종에서만 수행** |

---

## 6. 생성 및 수정한 파일

> 이번 대화에서 저장소를 **처음부터 생성**했으므로 99개 추적 파일 전부가 신규입니다.
> 아래는 주요 파일이며, 전체는 `git ls-files`로 확인하십시오.

### 6-1. 핵심 로직 (`src/core/`) — 전부 신규, 완료

| 파일 | 목적 | 핵심 | 추가 확인 |
| --- | --- | --- | --- |
| `core/model/types.ts` | 도메인 타입 | `Student`, `Seat`, `Division`, `EXCLUDED_STATUSES` | 불필요 |
| `core/model/normalize.ts` | 셀 정규화 | `parseNumberLike()`, `normalizeHeader()` | 불필요 |
| `core/excel/detectHeader.ts` | 점수 기반 헤더 탐색 | `FIELD_WEIGHT` (이름·번호 4점) | 불필요 |
| `core/excel/parseRoster.ts` | 행 읽기·종료 판정 | `BLANK_RUN_LIMIT = 3` | 불필요 |
| `core/excel/readWorkbook.ts` | SheetJS 유일 사용처 | `LIMITS` (20MB·20000행·512열·100시트) | 불필요 |
| `core/layout/viewpoint.ts` | 관점 180° 회전 | `toDisplay()` (자기 역함수) | 불필요 |
| `core/layout/groupIslands.ts` | 모둠 섬 생성·재편성 | `createGroupClassroom()`, `regroupFromSeats()` | 불필요 |
| `core/constraints/evaluate.ts` | 점수 계산 | `mixSideOf()` — 성별/구분 공용 | 불필요 |
| `core/constraints/diagnose.ts` | 탐색 전 모순 검출 | 11종 진단 코드 | 불필요 |
| `core/solver/seating.ts` | 자리 탐색 | multi-start SA, `swapPools` | 불필요 |
| `core/solver/partition.ts` | 인원 분할 | `partitionByCount()`, `arrangeSizes()` | 불필요 |
| `core/roster/divisionSplit.ts` | 구분 자동 분할 | `detectSplitPoints()`, `isConfident()` | 불필요 |
| `core/exportData/redact.ts` | 내보내기 축약 | `STUDENT_FACING`, `TEACHER_FACING` | 불필요 |
| `core/exportData/anonymize.ts` | 피드백용 익명화 | `anonymizeStudents()` | 불필요 |

### 6-2. 화면·인프라 — 전부 신규, 완료

`src/ui/screens/`(6개), `src/ui/components/`(9개), `src/store/useAppStore.ts`,
`src/lib/{log,storage,pwa,sample,solverClient}.ts`, `src/workers/solver.worker.ts`

### 6-3. 설정 — **확인 필요**

| 파일 | 상태 | 추가 확인 |
| --- | --- | --- |
| `src/config/links.ts` | 신규 | **필요 — `FEEDBACK_FORM`이 비어 있음** |
| `src/config/version.ts` | 신규 | 불필요 |
| `vite.config.ts` | 신규 | 불필요 (CSP 정의처) |
| `package.json` | 신규 | **필요 — `inspect` 누락, `fixtures` 깨짐 (8절)** |
| `playwright.config.ts` | 신규 | 불필요 |

### 6-4. 배포 자산 — 신규, **일부 확인 필요**

| 파일 | 추가 확인 |
| --- | --- |
| `site/index.html` | **필요 — 애드센스 `ca-pub-XXXX` 자리표시자** |
| `site/privacy.html` | 불필요 |
| `site/_headers` | 불필요 |
| `scripts/build-site.ts` | 불필요 |
| `.github/workflows/deploy.yml` | 불필요 (원격 연결 후 동작 확인 필요) |

### 6-5. 테스트 — 신규, 완료

`tests/unit/`(8파일 192개), `tests/e2e/`(4파일 28개), `tests/support/`(픽스처 2개)

### 6-6. 문서 — 신규, 완료

`README.md`, `PRIVACY.md`, `SECURITY_AUDIT.md`, `ARCHITECTURE.md`,
`EXCEL_IMPORT_SPEC.md`, `CONSTRAINT_ENGINE.md`, `TESTING.md`, `DEPLOY.md`, 본 문서

### 6-7. 자동 생성물 (git 미추적)

`dist/`, `node_modules/`, `test-results/`, `playwright-report/`

### 6-8. 저장소 밖 사본 (수동 복사, 자동 동기화 아님)

`I:\내 드라이브\2025-26고림고\교과업무\자리배치앱\` — 문서 + `dist` + 「먼저 읽어보세요.md」

---

## 7. 테스트 및 검증 결과

**모두 이 문서 작성 직전에 실제로 실행했습니다.**

| 명령 | 결과 |
| --- | --- |
| `npm run typecheck` | **통과** (종료 코드 0, 오류 0) |
| `npm test` | **192개 통과 / 0 실패** (8파일, 7.87초) |
| `npx playwright test` | **28개 통과 / 0 실패** (1.2분, chromium) |

### 단위 테스트 구성 (192개)

excel 28 · partition 29 · solver 31 · layout 20 · history 21 · groupIslands 24 ·
divisionSplit 24 · privacy 15+

### 개인정보 감사 결과 (E2E 자동)

- 전체 흐름에서 **앱 자신의 정적 파일 외 요청 0건**
- 학생 표식이 요청 URL·쿼리·본문에 **0건**
- 저장 끔 상태에서 새로고침 후 잔존 **0건**
- 유출 경로 탐침 17개 중 **서버 도달 1건** (알려진 prefetch 잔여, 이름 붙인 예외로 고정)
- 교차 출처 도달 **0건**

### 실제 파일 검증 (수동, 재현 가능)

```
1-1반 명렬표.xlsx   헤더 9행 · 25명 · 경고 0
1-2반 명렬표.xlsx   헤더 9행 · 25명 · 경고 0
1-3반 명렬표.xlsx   헤더 9행 · 24명 · 경고 0
1-78910 명렬.xlsx   헤더 2행 · 31명 · 경고 0  (한셀 저장본)
```

구분 자동 분할: 1-1반 12/13, 1-2반 12/13, 1-3반 11/13 — 모두 1순위가 진짜 경계

### 테스트하지 못한 영역 (미실행 — 통과로 추정 금지)

| 영역 | 상태 |
| --- | --- |
| **Firefox·Safari 전체** | **미실행.** 모든 측정은 Chromium |
| **실제 GitHub Pages 배포본** | **미실행.** 배포한 적 없음. `vite preview`로만 검증 |
| **PWA 설치 프롬프트** | **미실행.** 서비스워커 등록·오프라인 재적재는 검증했으나 설치 UI는 미검증 |
| **모바일·태블릿 실기기** | **미실행** |
| **PDF 한글 부제목 렌더링** | **미실행.** 제한사항으로 문서화만 함 |
| **애드센스 스크립트 실제 로드** | **미실행.** 주석 처리 상태 |
| **`npm ci` 클린 설치** | **미실행.** CDN 타르볼 의존성 때문에 CI에서 확인 필요 |

### 데이터 누수·lookahead 점검

**해당 없음.** 이 프로젝트에는 시계열 데이터·백테스트·신호/체결 분리 개념이 없습니다.
대응되는 점검은 「개인정보 유출 경로 점검」이며 위에 기록했습니다.

### 재현 조건

- Node 18+ (개발 시 v24.18.0 사용)
- `npm run test:e2e:install` 최초 1회
- **포트 4173에 다른 서버가 없어야 함** (아래 8-6)
- `cdn.sheetjs.com` 접근 가능 (`npm ci` 시)

---

## 8. 미해결 문제

### 8-1. `npm run inspect`가 package.json에 없음 — **우선순위 높음**

- **문제** `README.md:99`와 `TESTING.md:273`이 `npm run inspect -- "<경로>"`를 안내하지만 `package.json`에 해당 스크립트가 없습니다
- **영향** 문서를 따라 한 사용자가 즉시 오류를 만납니다. 실제 명렬표 인식 점검 도구를 못 씁니다
- **원인** `scripts/inspect-roster.ts`는 존재하는데 스크립트 등록만 누락
- **시도** 없음 (문서 작성 중 발견)
- **다음 조치** `package.json`에 `"inspect": "tsx scripts/inspect-roster.ts"` 추가
- **해결 전 피할 것** 없음

### 8-2. `fixtures` 스크립트가 존재하지 않는 파일을 가리킴 — **우선순위 중간**

- **문제** `"fixtures": "tsx scripts/make-fixtures.ts"` — 이 파일은 없습니다
- **영향** `npm run fixtures` 실행 시 오류. 다른 기능에는 영향 없음 (픽스처는 테스트가 코드로 생성)
- **원인** Phase 1 계획 단계의 잔재. 이후 `tests/support/neisFixture.ts`가 역할을 대신함
- **다음 조치** 스크립트 항목을 **삭제**할 것을 권합니다 (파일을 새로 만들 필요 없음)

### 8-3. 피드백 창구 미설정 — **우선순위 높음 (사용자 작업)**

- **문제** `FEEDBACK_FORM = ''`
- **영향** 「의견 보내기」에 링크 대신 안내 문구만 표시 (죽은 링크는 아님)
- **다음 조치** 사용자가 구글 폼 생성 후 주소를 채워야 함. `DEPLOY.md` 3절에 절차 있음
- **주의** 폼 파일 업로드는 **응답자 구글 로그인 필요**. 사용자에게 알렸으나 대안 미결정

### 8-4. 배포 미수행 — **우선순위 높음 (사용자 작업)**

- **문제** git 원격 없음, GitHub Pages 미설정
- **다음 조치** `DEPLOY.md` 1~2절. **push와 애드센스 신청은 사용자 계정으로 직접 수행해야 함**
- **해결 전 피할 것** 에이전트가 임의로 원격을 추가하거나 push하지 말 것

### 8-5. prefetch 잔여 유출 경로 — **우선순위 낮음 (해소 불가)**

- **문제** 동일 출처 `<link rel=prefetch>` 1건이 서버에 도달
- **원인** Chrome이 `prefetch-src`를 제거했고 `default-src`로 대체되지 않음
- **시도했으나 실패** `default-src 'none'`으로도 차단되지 않음 (실측)
- **현재 방어** 소스 스캔이 `rel="prefetch"`를 금지. E2E가 이름 붙인 예외로 고정
- **다음 조치** 브라우저에 통제 수단이 생기면 재검토. 지금은 조치 불필요

### 8-6. E2E가 포트 4173의 기존 서버를 재사용함 — **우선순위 중간**

- **문제** `reuseExistingServer: !process.env.CI`라서 로컬에 다른 서버가 4173에 떠 있으면 그것을 씁니다
- **영향** 이번 세션에서 실제로 발생했습니다. 이전에 띄운 미리보기 서버가 **소개 페이지**를 서빙 중이어서 E2E 28개가 전부 타임아웃(10분 초과)됐습니다
- **원인** `npm run build`가 `dist/`를 소개 사이트로 바꾸고, 그 상태의 서버가 남아 있었음
- **해결한 방법** 해당 프로세스 종료 후 재실행 → 28개 통과
- **다음 조치** E2E 실행 전 `netstat -ano | grep :4173` 확인. 근본 해결은 포트 변경 또는 `reuseExistingServer: false`
- **해결 전 피할 것** E2E 실패를 코드 문제로 단정하기 전에 반드시 포트를 확인할 것

### 8-7. Drive 사본이 수동 복사 — **우선순위 낮음**

- **문제** `I:\...\자리배치앱\`은 `cp`로 만든 스냅샷이며 자동 동기화되지 않습니다
- **영향** 저장소를 고치면 사본이 낡습니다
- **다음 조치** 배포 후에는 사본 대신 URL을 쓰도록 안내

### 8-8. 문서화된 미검증 제한사항 — **우선순위 낮음**

PDF 한글 부제목, Firefox 삭제 확인 제한, 모바일 편집 UX — 모두 `README.md` 「알려진 제한사항」에 기록. 실측 미수행

---

## 9. 다음 세션의 정확한 작업

### 작업 1 — 깨진 npm 스크립트 2건 수정 (가장 먼저, 가장 구체적)

- **목적** 문서가 안내하는 명령이 실제로 동작하게 만들기
- **확인할 파일** `package.json`(scripts), `scripts/` 디렉터리 목록, `README.md:99`, `TESTING.md:273`
- **수정할 파일** `package.json` 단 하나
- **정확한 변경**
  - `"inspect": "tsx scripts/inspect-roster.ts"` **추가**
  - `"fixtures": "tsx scripts/make-fixtures.ts"` **삭제** (대상 파일 없음, 대체 수단 존재)
- **실행할 테스트**
  1. `npm run inspect -- "I:/내 드라이브/2025-26고림고/교과업무/1-1반 명렬표.xlsx"`
     → 「학생 수: 25 / 경고 없음」이 나오고 **학생 이름이 출력되지 않아야 함**
  2. `npm run typecheck`
  3. `npm test`
- **완료 기준** 위 3개 모두 통과 + `package.json`의 모든 스크립트가 존재하는 파일을 가리킴
- **중단 기준** `inspect` 실행 시 학생 이름이 출력되면 즉시 중단하고 `scripts/inspect-roster.ts`의 마스킹 로직부터 점검
- **절대 하지 말 것** `scripts/make-fixtures.ts`를 새로 만들지 말 것 (테스트가 이미 코드로 픽스처를 생성함)

### 작업 2 — 사용자 배포 지원 (사용자 요청이 있을 때만)

- **목적** GitHub Pages 공개
- **확인할 파일** `DEPLOY.md`, `.github/workflows/deploy.yml`
- **수정 예상 파일** 없음 (사용자가 명령 실행)
- **완료 기준** 사용자가 `/`와 `/app/` 접속 확인
- **절대 하지 말 것** **에이전트가 `git remote add`·`git push`를 임의로 실행하지 말 것.**
  외부 공개는 사용자 결정이며 사용자 계정으로 수행해야 함

### 작업 3 — 피드백 폼 주소 반영 (사용자가 주소를 준 뒤에만)

- **확인할 파일** `src/config/links.ts`
- **수정 파일** 같은 파일의 `FEEDBACK_FORM`
- **실행할 테스트** `npx playwright test tests/e2e/feedback.spec.ts`
- **주의** 「설정 전에는 링크 대신 안내를 보여 준다」 테스트가 **깨집니다.** 주소를 넣으면 그 테스트를 「링크가 보인다」로 바꿔야 함
- **완료 기준** 링크가 새 탭으로 열리고, 쿼리스트링 금지 테스트 통과
- **절대 하지 말 것** 주소에 `?` 또는 `#`를 붙이지 말 것 (P5 위반, 테스트 실패)

### 작업 4 — 애드센스 (승인 후에만)

- **확인할 파일** `site/index.html`, `DEPLOY.md` 4절
- **수정 파일** `site/index.html`만
- **절대 하지 말 것** **`src/` 아래 어디에도 광고 코드를 넣지 말 것** (P12 위반). CSP가 차단하고 단위 테스트가 실패함

### 작업 5 — 다른 브라우저 검증 (여유가 있을 때)

- **목적** R5 해소
- **완료 기준** Firefox에서 「모든 정보 삭제」가 **「확인 불가」**로 정직하게 표시되는지 확인

---

## 10. 재개 명령 및 검증 명령

```bash
cd C:\Users\pc\dev\seat-planner
```

```bash
git status && git log --oneline -5 && git remote -v
```

```bash
npm run typecheck && npm test
```

```bash
netstat -ano | grep ":4173.*LISTENING"
```

> **E2E 실행 전 반드시 위를 확인하십시오.** 결과가 있으면 그 PID를 종료한 뒤 실행하십시오 (8-6).

```bash
npm run test:e2e
```

```bash
npm run build
```

```bash
npx tsx scripts/inspect-roster.ts "I:/내 드라이브/2025-26고림고/교과업무/1-1반 명렬표.xlsx"
```

### 산출물·보고서 경로

| 대상 | 경로 |
| --- | --- |
| 배포 폴더 | `dist/` (소개) · `dist/app/` (앱) |
| 감사 보고서 | `SECURITY_AUDIT.md` |
| 배포 절차 | `DEPLOY.md` |
| 테스트 목록·수동 점검표 | `TESTING.md` |
| 엑셀 인식 규칙 | `EXCEL_IMPORT_SPEC.md` |
| 알고리즘 설명 | `CONSTRAINT_ENGINE.md` |
| Playwright 실패 산출물 | `test-results/` (스크린샷·비디오는 **꺼짐**) |
| Drive 사본 | `I:\내 드라이브\2025-26고림고\교과업무\자리배치앱\` |

---

## 11. 세션 전환 위험 — 대화에만 있고 코드/문서에 없는 것

> 아래는 **압축·전환 과정에서 유실되기 쉬운 합의사항**입니다.

### 11-1. 사용자가 명시적으로 내린 결정 (코드에 흔적이 약함)

| 합의 | 근거 | 반영 위치 |
| --- | --- | --- |
| 프로젝트를 **로컬 드라이브**에 둔다 | Drive 동기화가 `node_modules`로 느려짐 | 경로 자체 (문서에 이유 없음) |
| 배포는 **GitHub Pages** | 사용자 선택 | `DEPLOY.md` |
| 광고는 **소개 사이트에만** | 사용자 선택(추천 수용) | `DEPLOY.md`, `site/index.html` 주석 |
| 서비스워커를 **제거하지 말고 등록 코드를 넣는다** | 사용자 선택 | `src/lib/pwa.ts` |
| CSP를 **최대한 조인다** | 사용자 선택 | `vite.config.ts` |
| 우선 기능 4가지: 제외·관점전환·모둠대안·공개모드 | 사용자 선택 | 코드에 반영, 우선순위 근거는 미기록 |

### 11-2. 사용자 맥락 (문서에 없음)

- 사용자는 **고림고등학교 교사**, 통합과학1 담당, 1학년 여러 학급
- 실제 명렬표는 `I:\내 드라이브\2025-26고림고\교과업무\`에 있으며 **절대 커밋 금지**
- 개발 중 이 파일들로 파서를 검증했고, 검증 스크립트는 **이름을 출력하지 않도록** 만들어져 있음
- 사용자는 다른 교사들에게 공유할 계획이며 피드백을 받고 싶어 함

### 11-3. 판단 근거가 대화에만 있는 것

| 사항 | 유실되면 생길 일 |
| --- | --- |
| **XLSX 접두사 방어를 일부러 안 넣음** | 다음 세션이 「보안 강화」로 착각해 추가 → 정상 메모 손상. **4-6 참조** |
| **`frame-ancestors`를 meta에서 일부러 뺌** | 「빠졌네」 하고 되넣음 → 무효 지시문이 거짓 안심을 줌. **4-5 참조** |
| **prefetch 1건은 의도적으로 허용된 예외** | E2E의 `KNOWN_UNBLOCKABLE`을 버그로 오인 → 무리한 우회 시도. **8-5 참조** |
| **모둠 응집을 감점으로 하면 실패한다** | 감점 방식으로 되돌림 → 사용자가 겪은 버그 재발. **4-3 참조** |
| **미사용 의존성 6개를 감사에서 제거함** | dnd-kit·radix를 다시 추가 → 공격 표면 증가 |

### 11-4. 측정 방법에 관한 함정 (반드시 보존)

**Playwright의 `request` 이벤트 발생은 「바이트가 나갔다」는 뜻이 아닙니다.**
Chrome은 요청 이벤트를 올린 뒤 CSP로 실패시킵니다. `requestfinished`(전달)와
`requestfailed`(차단, 사유)를 구분하지 않으면 **차단된 교차 출처 요청을 유출로 오판**합니다.
이번 세션에서 실제로 오판할 뻔했습니다. `tests/e2e/privacy-audit.spec.ts`의 `record()`가
이 구분을 구현하고 있으니 **단순화하지 마십시오.**

### 11-5. 이 문서에 담지 못한 것

- 각 커밋의 전체 diff (→ `git show <hash>`)
- Playwright·Vitest 원본 로그 (보존 안 함. 재실행으로 획득)
- 감사 당시 「수정 전」 실측 원본 (요약만 `SECURITY_AUDIT.md` 15절에 존재)
- 대화에서 논의만 하고 채택하지 않은 UI 시안들
